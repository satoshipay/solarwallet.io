{
  "scripts": {
    "deploy": "now deploy --public --scope=solar .",
    "deploy:www-redirect": "cd redirect/ && now deploy --public --scope=solar .",
    "dev": "npx serve ."
  },
  "dependencies": {}
}
